spring:
  application:
    name: transaction-service
  cloud:
    config:
      enabled: false
  data:
    mongodb:
      uri: mongodb://localhost:27017/transactiondb
server:
  port: 8082

transaction:
  free-limit: 5  # Establece el límite de transacciones gratuitas

logging:
  level:
    org.springframework: INFO
